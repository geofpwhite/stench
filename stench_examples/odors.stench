

dump "f" ;
odor dump_and_return(smell: string, other: int) int {
    dump smell;
    dump other;
    dump smell;
    pileup i=0;i<10;i=i+1{
        dump i;
    }
    other
}

x = (sniff dump_and_return("x", 5));

dump ("x is " + x);